# Parser Fixtures

Parser fixtures are broad syntax coverage cases. They verify that the parser and AST projection remain tolerant across Oracle PL/SQL constructs.

They are not lineage ground truth unless a matching expected JSON file is added under a synthetic or curated correctness fixture.

