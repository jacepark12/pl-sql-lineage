# Public Fixtures

Run the fetcher from the repository root:

```sh
python3 scripts/fetch_public_fixtures.py
```

Public fixtures are pinned by upstream commit. Use them for parser coverage and object-resolution coverage. Do not treat them as lineage correctness tests until expected JSON has been written by hand.

