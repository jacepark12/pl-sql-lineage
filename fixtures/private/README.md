# Private Fixtures

This directory is reserved for anonymized real-world Oracle PL/SQL.

Rules:

- Do not store raw production SQL.
- Replace schema, table, column, package, procedure, literal, and comment content with deterministic pseudonyms.
- Preserve dependency shape, statement order, and PL/SQL control structure.
- Keep this directory out of public distribution.

